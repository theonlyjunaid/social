{
  "name": "social-backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir ./build",
    "start": "bun --watch run ./build/index.js"
  },
  "dependencies": {
    "hono": "^4.6.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}